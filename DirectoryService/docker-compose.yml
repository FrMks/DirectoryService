services:
  directory-service:
    container_name: directory-service
    build:
      context: .
      dockerfile: src/Dockerfile
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DirectoryServiceDb: Server=postgres;Port=5432;Database=directory_service_db;User Id=postgres;Password=postgres;
      ConnectionStrings__Seq: http://seq:5341
      ConnectionStrings__Redis: redis:6379
      Swagger__ServerUrl: http://localhost:8080
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      seq:
        condition: service_started
      redis:
        condition: service_started

  postgres:
    container_name: postgres
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: directory_service_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d directory_service_db"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

  seq:
    image: datalust/seq:latest
    container_name: seq
    restart: unless-stopped
    environment:
      ACCEPT_EULA: "Y"
      SEQ_FIRSTRUN_ADMINUSERNAME: "admin"
      SEQ_FIRSTRUN_ADMINPASSWORD: "YourStrong!Pass1"
    ports:
      - "8081:80"
      - "5431:5341"
    volumes:
      - seq-data:/data

  redis:
    image: redis:alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  postgres-data:
  seq-data:
