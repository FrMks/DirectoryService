DirectoryService repository contains a Next.js client and .NET 10 backend. Backend has DirectoryService and FileService bounded contexts. FileService uses Vertical Slice Architecture with Minimal API slices in Core; infrastructure implementations are Postgres and S3/MinIO. Contracts projects contain transport DTOs and must remain separate from domain/internal implementations. Main backend solution: backend/DirectoryService.sln.