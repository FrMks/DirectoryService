# Context refresh cadence

User requested context-memory refresh every 10 user prompts to reduce repeated repository discovery. Baseline reset: current user request counts as prompt 0 for the next interval. At each interval, update relevant Serena memories with stable architecture, key commands, compatibility traps, and verified test state. Update AGENTS.md only for durable repository-wide navigation or rules, not transient progress.
