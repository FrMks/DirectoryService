Before completion, build the smallest affected project, then run focused tests; use the backend solution build/test for cross-project changes. Check git diff and preserve unrelated user changes. For API contract changes, inspect Contracts, endpoint/Presentation, consumer, and integration tests together. For project moves/renames update solution, project/test references, and Docker paths.